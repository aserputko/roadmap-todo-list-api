meta {
  name: Login
  type: http
  seq: 1
}

post {
  url: {{API_URL}}/login
  body: json
  auth: inherit
}

body:json {
  {
    "email": "{{USER_EMAIL}}",
    "password": "{{USER_PASSWORD}}"
  }
}

settings {
  encodeUrl: true
}
